__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Never commit a node's credentials. The bearer token is the only durable
# state the service keeps; everything else is re-obtained from the server.
*.token
token
.env
